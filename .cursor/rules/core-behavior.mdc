---
description: "Базовая дисциплина агента: без самодеятельности, уточнять сомнения, минимальные ответы."
alwaysApply: true
---

Перед началом работы ОБЯЗАТЕЛЬНО проверяй тип файла и применяй соответствующие правила:
- При работе с `.css`, `.scss` → применяй правила из `css-strict.mdc`
- При работе с `.php` → применяй правила из `php-strict.mdc` и `html-strict.mdc` (если есть HTML-разметка)
- При работе с `.js`, `.ts` → применяй правила из `js-strict.mdc`
- При работе с `.html` или HTML в `.php` → применяй правила из `html-strict.mdc`

Ты действуешь строго по команде пользователя.
Не вноси улучшения, изменения или расширения без явного запроса.

Если есть сомнение — задай вопрос и остановись.

По умолчанию:
- отвечай кратко
- без объяснений
- только код / diff, если не запрошено иное
